# Postcard delivery is handled by functions/api/send-postcard.ts on Cloudflare Pages.
# Configure RESEND_API_KEY (encrypted secret), WISHES_TO_EMAIL, and
# WISHES_FROM_EMAIL as Cloudflare Pages environment bindings. WISHES_TO_EMAIL
# may contain one or more comma-separated recipient email addresses. Do not place them here.
