# Where postcard wishes are delivered. Any endpoint accepting a JSON POST works
# (Formspree, Basin, a Cloudflare Worker, a Google Apps Script webhook).
# Leave unset and wishes are simply kept in the visitor's browser.
VITE_WISH_ENDPOINT=
